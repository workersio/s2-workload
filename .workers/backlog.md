# Backlog
- active: 18
- areas: { appends: 4, reads: 1, lifecycle: 2, retention: 2, control-plane: 3, runtime: 6 }
- top-score: 600
- threshold: 20

Merged 2026-07-06 from the 5-beat cartographer fan-out (producer #8): 35 scout
shards deduped to 23 rows; 4 promoted same-episode (rows deleted per spec);
scores use the top-scored variant of each merge group, provenance unioned.
Ranking audited by strategy-critic same episode (2 rescores applied, 1 seam
row added, top-3 provenance source-verified).

## Active (sorted by score, descending)

| score | candidate | area | L·I·O·N·R/C | provenance | source | notes |
|-------|-----------|------|-------------|------------|--------|-------|
| 600 | append-vs-stream-delete-ack-atomicity — append racing DELETE can be acked-then-gone: delete is two-phase (streamer terminal-trim → mark_stream_deleted), non-atomic; auto-create first-appends racing delete can resurrect or double-create | lifecycle | 3·5·5·4·4/2 | "#469 60848a5; streams.rs:337-379 (two-phase delete, separate txns — critic-verified); core.rs:310-345 (auto-create join); records.rs:735 (upstream auto-create worry)" | scout-commits | merged: auto-create-append-delete-interleave (480). oracle: every 200-ack readable-or-deletion-pending-error, never ack-then-gone, never 500 |
| 480 | ensure-reconfigure-config-clobber — empty PUT on an existing customized stream silently resets config to basin defaults; PATCH must reach the live streamer (advise dropped for Initializing slots); config survives restart | control-plane | 4·4·4·5·3/2 | "streams.rs:133-151 (Ensure merge = clobber); streams.rs:321-323 + core.rs:283-294 (advise_reconfig skips Initializing); config.rs:12-24 (PATCH clear semantics); mixed.rs:117 (only Ready-streamer coverage)" | scout-api | merged: lost-to-initializing (tests 288), patch-clear (docs 270), under-traffic (runtime 240). clobber corridor is promise-shaped — decide contract-vs-bug before asserting direction |
| 480 | encrypted-stream-key-mismatch-cycle — wrong key/algorithm/missing header on a cipher-configured stream: stable 4xx classes, never plaintext leak, stream still readable with the right key after; 5 fixes churned this mapping | appends | 3·4·4·5·4/2 | "#421 055708b, #422 2fd8dbf, #412 ecc8621; storage/src/record/encryption.rs:117-132; live in append.rs/read.rs/records.rs" | scout-commits | plaintext command records (fence/trim) on encrypted stream is the spicy arm |
| 450 | list-pagination-census — start_after-chained walk returns exactly the created set (no dupes/omissions, has_more truthful, deleted_at surfaced); read-your-writes: stream visible via GET config visible in LIST | control-plane | 2·3·5·5·3/1 | "handlers/v1/streams.rs:61-71; streams.rs:35-77 (has_more limit-overflow; Remote filter at :52 vs txn commit — RYW lag window); #448 (cursors before prefix)" | scout-api | merged: under-churn (docs 288). C=1 — cheapest row in the pool |
| 400 | doe-wrongful-delete — delete-on-empty must never delete a stream with an acked write after the cutoff or unexpired records; disarm-via-reconfigure leaves stale deadline KV; restart reconstructs last-write from create_ts (stale) | retention | 4·5·4·5·4/4 | "#495 37cb03d (recheck before terminal trim), #314, #330; streams.rs:295-310 (arming only when prior min_age None — rearm gap incl. init-file path); stream_doe.rs:105-121 (cutoff check-then-act); streamer.rs:448-511 (snapshot re-check dance); core.rs:110-111,161 (restart seam)" | scout-commits | merged: disarm-reconfigure (533), wrongful-delete (docs 500), vs-acked-append (tests 300), init-file-rearm-gap (runtime 192). critic 2026-07-06: C rescored 3→4 (533→400) — 600s deadline pad (streamer.rs:557-568) + 60s tick = ≥10min wall per observation; depth-10 is multi-hour; needs a long-budget design  promoted-adjacent 2026-07-07: ONLY the across-recreate stale-deadline corridor is attacked by doe-stale-deadline-across-recreate (lifecycle promise) — CORRIDOR WENT RED (executor #18, 3/3 deterministic wrongful deletes, seed 2492750010; runs/doe-stale-deadline-across-recreate.md) => stale-deadline mechanism CONFIRMED live, raising L on the sibling corridors; remaining here (critic-flagged, do not archive as covered): same-incarnation disarm-reconfigure stale deadline (streams.rs:304-306 arms only when prior min_age None; upstream test stream_doe.rs:757-801 pins the stale entry) + wrongful-delete vs acked-append recheck race + init-file rearm gap |
| 384 | malformed-record-decode-append-surface — malformed envelope/command records (empty header names, truncated bytes, garbage fence/trim bodies) must 4xx cleanly, never panic or wedge the streamer | appends | 3·4·4·4·4/2 | "#446 722ecdd, #362 e2245e3, #288 58e81ab; unreachable! cluster streamer.rs:538-544; JSON vs s2s/proto routing handlers/v1/mod.rs:21" | scout-commits | oracle: every case clean 4xx AND stream stays appendable/readable after |
| 360 | control-plane-occ-conflict-storm — concurrent create/reconfigure/delete on same stream+basin: no raw txn conflicts as 500s, final config equals exactly one submitted value (no field interleave) | control-plane | 4·3·3·4·5/2 | "#466 9af4a67; error.rs:88-89 (TransactionConflictError mapping); SerializableSnapshot txns streams.rs:86,267" | scout-commits | resonates with the harness's own #1 platform failure mode (OCC) |
| 320 | db-closed-health-liveness — when slatedb dies mid-run, /health flips 503 within bound and appends fail fast (no hang, no 200-while-dead) | runtime | 2·4·5·4·4/2 | "handlers/mod.rs:19-23 (db_status gate); streamer.rs:697-703 (pending appends errored, loop exits); core.rs:198-215 (dead-client replacement)" | scout-runtime | C=2 only as the fatal-fault arm of s3-endpoint-fault-plane's stub |
| 320 | retention-ttl-expiry-boundary — age retention is per-record TTL fixed at write; expired records must vanish without mid-stream gaps, 416 storms, or tail regression; reconfigure ⇒ non-monotone expire_ts in seq order | retention | 4·4·4·5·3/3 | "streamer.rs:1021-1037 (Ttl::ExpireAfter at append — no retro-apply); config.rs:68-84 (default Age 7d, age=0 invalid); streamer.rs:948-962; read.rs:290-329 (resolve_timestamp)" | scout-tests | merged: age-expiry-boundary (docs 320). zero expiry coverage in lite/tests; distinct mechanism from trim (TTL vs trim-point key) |
| 320 | s2s-append-session-poisoning — in a pipelined S2S append session, after the first rejected input zero subsequent inputs are sequenced or acked; acks strictly ordered and contiguous | appends | 3·4·4·5·4/3 | "append.rs:113-135 (admit returns None once poisoned), :226-247 (reject extends durability dependency); records.rs:405-455; api/src/v1/stream/s2s.rs (framing)" | scout-api | critic 2026-07-06: L rescored 4→3 (427→320) — poisoning is typestate-enforced with asserts (append.rs:113-135,215,234); real risk is framing-level. first black-box exercise of the s2s path; poison exclusion is the new surface |
| 300 | s3-endpoint-fault-plane — under injected object-store faults (5xx bursts, latency, lost PUT responses) via AWS_ENDPOINT_URL_S3 → guest-local python S3 stub, no false acks and no phantom reads after heal | runtime | 3·5·4·5·4/4 | "server.rs:288-300 (endpoint override + allow_http on http://); streamer.rs:690-704 (DurabilityStatus Err path); sim/src/s3.rs:58,113-128 (If-Match CAS + lost-response PUT dedup seam, upstream tests in-sim only)" | scout-runtime | opens the dependency-fault plane — the areas-doc open flank; stub amortizes db-closed + sl8 rows |
| 288 | append-admission-saturation — at the 1MiB --append-inflight-bytes clamp floor, concurrent max batches + mid-wait client disconnects never leak permits or wedge admission | runtime | 3·3·4·4·4/2 | "core.rs:58-63 (clamp floor = total serialization); streamer.rs:815-838 (permit held across durability wait); append.rs:114-135 (cancellation seam); metrics.rs:6-10 (permit latency metric)" | scout-runtime | probe append + /metrics latency as oracle |
| 225 | batch-caps-boundary-atomicity — 1000-record/1MiB-metered batch caps: rejection atomic (tail unchanged), exact boundary accepted, 4xx not 5xx | appends | 2·3·5·3·5/2 | "caps.rs:10-16; record/mod.rs:208-216 (metered formula: 8 + 2·headers + bytes); api/src/v1/stream/mod.rs:371-373" | scout-docs | |
| 216 | graceful-drain-window — during the 10s SIGTERM drain: new connections refused, in-flight sessions keep acking, drain-window acks durable after restart | runtime | 4·3·3·3·4/2 | "server.rs:356-382 (graceful_shutdown 10s, no db.close after serve)" | scout-runtime | scope to drain ADMISSION semantics — the durability leg overlaps the covered SIGKILL family |
| 216 | sse-session-termination-taxonomy — what an in-flight SSE follower observes when its stream is deleted/terminal-trimmed or the DB dies mid-follow: a clean `error` event or [DONE], never a silent hang or a post-deletion record | reads | 3·3·4·4·3/2 | "critic 2026-07-06 missing-seam finding; SSE event taxonomy handlers/v1/records.rs (batch/ping/error events + [DONE]); db-death path streamer.rs:697-703" | critic | db-closed-health-liveness covers appends/health only; this is the read-session half |
| 192 | basin-deletion-crash-resume-recreate — SIGKILL mid basin-deletion (cursor mid-page): deletion resumes and completes, recreate yields empty basin, creates during pending rejected | lifecycle | 3·4·4·4·3/3 | "bgtasks/basin_deletion.rs:67-112 (hand-rolled cursor advance/reset, rescan branch at 98-106); control_plane/basin.rs:371 (in-process only)" | scout-tests | fold into the delete/recreate family when promoted |
| 192 | sl8-backpressure-knobs — with SL8_MAX_UNFLUSHED_BYTES / L0 knobs squeezed, sustained load degrades to bounded latency, never permanent stall or crash with /health 200 | runtime | 2·4·4·4·3/2 | "server.rs:178-184 (any slatedb Settings field via SL8_* env); README.md:160-168" | scout-runtime | R discounted: slatedb 0.13.1 field list unverifiable locally — confirm names before building |
| 192 | streamer-dormancy-wake-burst — append+read burst landing exactly at 60s-idle streamer eviction: no "missing in action" 500s, no lost follow sessions | runtime | 3·4·4·4·3/3 | "#404 3fcfcee, #270 af1d2ed, #210 fa39551, #301 df66927; DORMANT_TIMEOUT streamer.rs:55 (hardcoded 60s); dormancy select-loop streamer.rs:614-708; dead-slot checks core.rs:201,248,291" | scout-commits | merged: dormancy-respawn-burst (tests 72). COST: needs real 60s idle per cycle — stagger many streams to amortize |

## Archive (no loop agent reads this)

Promoted 2026-07-07 (producer #9, batch #9 hard-commit): stream-delete-recreate-resurrection (667) -> promises/stream-delete-recreate-resurrection.md (3 rungs: fresh-identity, kill-mid-purge, doe-stale-deadline-across-recreate); control-plane-acked-ops-durable (480) -> promises/control-plane-acked-ops-durable.md (2 rungs: ack-then-kill, baseline).

- reads: bytes-budget-record-integrity (api 288) folded into the
  reads-never-lose-observed-records ladder — it is the untested bytes-budget
  arm of reads-tail-last-event-id-resume (records.rs:62), already a recorded
  residual in that promise; variations live in ladders, not here.
- durability/fencing: fencing-412-token-disclosure (critic seam) folded into
  the fencing-baseline rung as required pin #2 — not a separate row.
- promoted 2026-07-06 (producer #8, rows deleted): match-seq-num-cas-ledger
  (1250 → promise cas-appends-exactly-once), until-clamp-tailoffset-read-window
  (800 → promise reads-honor-request-windows, incl. read-tail-offset-trim-clamp
  merge, commits 270), timestamp-monotonicity (750, merged docs 750 + api 600 +
  tests 180 → promise timestamps-never-regress), trim-durability (640, carried
  critic direction → promise trim-is-final).
